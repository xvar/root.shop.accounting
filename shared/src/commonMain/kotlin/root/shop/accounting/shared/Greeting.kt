package root.shop.accounting.shared

// Stub for the setup phase: real domain models (Product, StockMovement,
// Reservation, Order, ...) land here once the per-feature architecture is
// worked out — see docs/design.md.
object Greeting {
    fun message(): String = "store-service shared module is wired up"
}
